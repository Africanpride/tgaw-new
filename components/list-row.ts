/**
 * Shared base for list rows. System rule: cards/shells use `rounded-xl`,
 * rows and inner panels use `rounded-lg`. Compose with context-specific
 * layout, padding, background, and hover classes via `cn()`.
 */
export const listRowClass = "rounded-lg border transition-colors";
